package chap16

import (
	"fmt"
	"testing"
)

func TestGetFreq(t *testing.T) {
	myMap := make(map[string]int)
	book := []string{"Once", "upon", "a", "time", "time", "is", "a", "Once"}
	fmt.Println(GetFreq(&myMap, book, "Once"))
	fmt.Println(GetFreq(&myMap, book, "time"))
	fmt.Println(GetFreq(&myMap, book, "upon"))
}
